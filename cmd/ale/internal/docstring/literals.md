---
title: "core literals"
description: "literal values for booleans, null, and special floating-point values"
names: ["true", "false", "null", "+inf", "-inf", "nan"]
usage: "(literal values)"
tags: ["data"]
---

These names are predefined literal values. `true` and `false` are Ale's boolean values. `null` is the empty list or null value. `+inf`, `-inf`, and `nan` are special floating-point values.

#### An Example

```scheme
[true false null +inf -inf nan]
```
