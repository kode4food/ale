// Package env provides namespace and binding management for the Ale runtime.
// It implements lexical scoping, symbol resolution, and environment chaining
// that supports nested scopes, closures, and public/private visibility. This
// is the foundation for global storage and lookup throughout the Ale
// interpreter.
package env
